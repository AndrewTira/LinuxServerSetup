apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bitwarden
  namespace: argocd
spec:
  project: default
  destination:
    namespace: bitwarden
    name: in-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://charts.bitwarden.com/
    chart: self-host
    targetRevision: "2025.4.2"
    helm:
      values: |
        global:
          domain: bitwarden.example.com

        controller:
          service:
            type: ClusterIP

        ingress:
          enabled: true
          className: nginx
          annotations:
            nginx.ingress.kubernetes.io/ssl-redirect: "true"
            nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
          hosts:
            - host: bitwarden.example.com
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - bitwarden.example.com
              secretName: bitwarden-tls  # Ensure this TLS secret exists

        admin:
          email: "admin@example.com"
          password: "changeme123"  # Use a secret in production

        database:
          enabled: true
          mssql:
            enabled: true
            password: "YourStrongP@ssw0rd!"  # Replace with a secure value or reference a secret
            image:
              tag: "2025.4.2"
            persistence:
              enabled: true
              storageClass: "standard"  # Adjust to match your cluster’s default
              accessMode: ReadWriteOnce
              size: 10Gi
