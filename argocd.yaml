apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-helms
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/AndrewTira/LinuxServerSetup
    targetRevision: HEAD
    path: argocd-helms
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
